{
  "name": "aily-coder-libraries",
  "version": "0.1.0",
  "private": true,
  "description": "Package Arduino libraries and synchronise them to the Coder npm registries",
  "scripts": {
    "sync": "node scripts/sync-libraries.mjs",
    "test": "node --test tests/node/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1127.0",
    "semver": "^7.7.2",
    "undici": "^6.21.3"
  }
}
